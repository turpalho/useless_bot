# useless_bot

QUICK START
-----------


Делаем cp .env.dist .env и добавляем необходимые токены
```
    .env
    .env.dist
```

Запускаем docker-compose

```
    $ docker-compose build
    $ docker-compose up -d
```
